const MobileViewButton = () => {
  return (
    <a
      href="#"
      className="view_btn mobile_only flex"
      style={{
        visibility: "hidden",
      }}
    >
      View
    </a>
  );
};

export default MobileViewButton;
